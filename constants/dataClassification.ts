/**
 * Data Classification Constants
 * Used for categorizing data sensitivity levels
 */

export enum DataClassification {
  PUBLIC = 'public',
  INTERNAL = 'internal', 
  RESTRICTED = 'restricted'
}

export default DataClassification;
