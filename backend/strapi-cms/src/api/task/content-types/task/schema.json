{
  "kind": "collectionType",
  "collectionName": "tasks",
  "info": {
    "singularName": "task",
    "pluralName": "tasks",
    "displayName": "Task",
    "description": "Project tasks - synced with backend Prisma"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text"
    },
    "status": {
      "type": "enumeration",
      "enum": ["TODO", "IN_PROGRESS", "REVIEW", "DONE", "CANCELLED"],
      "default": "TODO"
    },
    "priority": {
      "type": "enumeration",
      "enum": ["LOW", "MEDIUM", "HIGH", "URGENT"],
      "default": "MEDIUM"
    },
    "dueDate": {
      "type": "datetime"
    },
    "projectId": {
      "type": "integer",
      "required": true
    },
    "assigneeId": {
      "type": "integer"
    },
    "strapiId": {
      "type": "string",
      "unique": false
    },
    "backendId": {
      "type": "integer",
      "unique": false
    }
  }
}
